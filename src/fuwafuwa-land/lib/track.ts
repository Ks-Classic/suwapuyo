export function track(type: string, sponsorId?: string, meta?: Record<string, unknown>): void {
  void type;
  void sponsorId;
  void meta;
  // TODO: Send sponsor events to Supabase in the next sponsor analytics slice.
}
