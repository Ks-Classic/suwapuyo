# すわぷよ アプリ開発セッション引継ぎ（2026-07-13）

このファイルは、プロセスエコノミー／ブランド検討とは切り離して、アプリ実装を継続するためのコピー用プロンプトである。

## 新しい実装セッションへ渡すプロンプト

```text
あなたは「すわぷよ」のアプリ実装担当です。このセッションでは、プロセスエコノミー記事・SNS戦略・キャラクターブランドの検討を主目的にせず、SSoTに沿って安全に実装・検証・必要時のみデプロイしてください。

最初に必ず読むもの（推測で実装しない）:
1. AGENTS.md
2. docs/30_suwapuyo/02_todo.md（v2.8、TODOの正）
3. docs/70_すわぷよ・ユアタイム統合仕様/00_案内.md
4. TODOの対象セクション冒頭が指定するSSoT
5. git log main と git status

現状:
- main には Claude Code による優先16項目の実装が入っている。起点は 578e84d、直近のドキュメント追加は c9c9593。
- 完了済み（再実装しない）: CHAR-301/303/304、BOOTH-104/105、CHECKIN-102〜105、MAKER-102〜105、ONBOARD-306/307、LINE-202、Vitest設定修正。
- その時点で tsc --noEmit、npx vitest run（20 files / 93 tests）、npm run build は green と報告済み。自分の変更後は必ず再実行して確認する。
- 本番は Cloudflare Pages の `suwapuyo`。URL は https://suwapuyo.pages.dev 。Vercel の同名プロジェクトは旧デモで、本番ではない。
- 本番デプロイが必要なコード変更だけ、`npx wrangler pages deploy dist --project-name=suwapuyo --branch=main` を使う。ドキュメントだけの変更ではデプロイしない。
- 作業ツリーには他担当の未コミット変更があり得る。自分の変更以外は絶対に戻さない・混ぜてコミットしない。

最優先の未着手:
1. CHAR-302 の配線。
   - `src/shared/progressStore.ts` の `rerollUnpinnedSlots()` 自体とテストはあるが、「新しいゲーム開始」への接続は未実施。
   - 先に木幡さんへ、次のUXでよいか確認する。承認前に配線実装はしない。
     * 明示的な「新しいゲーム」を選んでキャラ選択へ入った一度だけ、固定していない枠を再抽選して表示する。
     * 画面最上部は、いま見えている4人で即開始できる主CTA（例: 「この4人ですぐ遊ぶ」）。開始時に再抽選しない。画面と実プレイのキャラを必ず一致させる。
     * 近くに「おまかせで選び直す」を置き、押した時だけ全体を再抽選して表示更新する。
     * 個別選択・固定/解除は、その下で可能にする。
   - mount のたびの再抽選や、主CTAクリック時の見えない再抽選は避ける。

明確な保留（勝手に実装しない）:
- ONBOARD-303: 子ども単独利用の actor_type と保護者確認年齢。要確認-011の決定後。
- LINE-204〜209: リッチメニュー画像、Messaging API登録、実機LINE確認。外部素材・実機・権限が必要。
- BOOTH-102/103/106/107: YourTIME運営との名称照合と正式会場図待ち。
- MAKER-101/106: やす・ツナマヨのプロフィール、相談先、SLA確定待ち。
- CHECKIN-106/107: 本番schema、署名、RLS。仕様上のGate通過前は着手禁止。

実装規律:
- 変更前に対象SSoTを読み、TODOのステータス・根拠も同期する。
- UXは「最上部で迷わず開始、選択は任意で深掘り」を原則にし、表示と結果を不一致にしない。
- 個人データ、同意、QR、レポートの保存境界を仕様より広げない。
- ユーザーに見えるバグを直す場合、可能なら `docs/80_process-economy/evidence/` にPIIを含まない再現・修正後の証跡を残す。生の会話ログや秘密情報はGitに入れない。
- AGENTS.md のモデル使い分け（A: 高難度判断、B: 実装、C: 定型）に従う。これは開発時のモデル選定であり、アプリ機能としてGPTを呼び出す話ではない。

完了時の最低確認:
- npx tsc --noEmit
- npx vitest run
- npm run build
- git diff --check
- 変更対象だけをコミットし、コードの本番デプロイをした場合はURL・コミット・検証結果を明記する。
```

## このスレッドの扱い

以後このスレッドは、やすキャラ／ツナやすのキャラクターブランド、公開制作過程、公式記事・SNSの編集方針を扱う。アプリ実装は上の引継ぎを新しいセッションに貼って進める。
