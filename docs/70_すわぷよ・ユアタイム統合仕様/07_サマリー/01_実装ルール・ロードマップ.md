# 実装ルール・ロードマップ

> 最終更新: 2026-07-13
> 開発エージェントへの実行時の正本: リポジトリルート`AGENTS.md`

## 1. 実装判断

全面書き直しはしない。ゲームコアと世界観資産を残し、入口・情報設計・保存境界・アクセシビリティを作り直す。

| 対象 | 判断 | 理由 |
|---|---|---|
| Pixi盤面・連鎖・音 | 再利用 | ゲーム価値が既にある |
| 村背景・キャラクター | 再利用・再編集 | ブランドDNA |
| 家族アンケート状態遷移 | 部分再利用 | 画面遷移の土台だけ残し、旧party・人数・性別・流入元・健康関係者ステップは削除 |
| 家族アンケート表示 | 全面リメイク | 汎用フォーム感が強い |
| 体操ホスト設定 | 再利用 | キャラと体操の対応がある |
| 体操画面・完了判定 | 作り直し | 待機だけで完了になる |
| `progressStore` | fallbackへ変更 | cloud SSoTではない |
| `track()` | 作り直し | 現在noop |
| スタンプ・ミッション演出 | 再利用 | 獲得体験の質が高い |
| 出展者レポート部品 | 再利用 | 集計の物語がある |
| レポートレイアウト | リファクタ | スクロール・密度・A11yに問題 |
| LINEデモメニュー | 作り直し | 実LIFFゲートではない |

## 2. 先に解決する仕様矛盾

1. ゲーム・記録・会場マップ・ブース紹介を含む全利用者機能でLINE友だち追加を必須とする（決定-003）。公開Webの会場案内を作る場合は本仕様の改訂として別途起案し、現時点では例外を設けない。
2. 配信先はCloudflareを第一候補とし、旧Vercel記述を現行判断に使わない。
3. 既存の隠しキャラを廃止し、全キャラクターを常時見せる。アンケート完了で全キャラを一括登場させる（決定-019）。
4. `app_users`を中心にアンケート、ゲーム、体操、ブース接点を結ぶ。
5. 出展者レポートをデモに含めるが、サンプル値を実績と呼ばない。
6. 初回登録は`利用目的 → 誰が遊ぶか → 子どもの人数 → 全員の生まれ年・月と性別 → 確認`とする。年月と性別を継続保存し、遊び方は初回から外す（決定-031〜034）。
7. ブース紹介はアンケート個別最適化を行わず、体操テーマと公平ローテーションを使う。

## 3. 実装フェーズ

日程は2026-08-02のYourTIME本番から逆算した仮決定であり、フェーズ0のレビューで確定する。外部Gateの期日（仮決定）: H-01（Cloudflare）=7/16、H-02/H-03（LINEチャネル）=7/18、H-08（ライセンス確認）=7/20、H-10（料金プラン）=7/22、H-06（ポリシーURL）=7/25。

### フェーズ0 — 仕様と既存不具合の固定（〜7/13・仮決定）

- 本仕様をレビューして確定。
- 旧仕様のstatus台帳を作る。
- キャラ登場単位、質問項目、MVP画面を確定。
- 年齢帯別体操のルール・説明・注意文を監修し、version付きで固定する（要確認-009）。
- 現行画面のスクロール、未回答既定値、日付誤記を修正対象として登録。

完了条件: 実装者が旧資料を推測で選ばずに済む。

### フェーズ1 — コードファーストUIデモ（7/14〜7/19・仮決定）

- 新しいアプリシェルとホーム。
- 同意ステップと認証・友だち追加案内・キャラ選択・ミッション画面（`02_体験設計/05` §12〜16）。
- 会話型アンケート。
- 保存年月からの年齢帯算出と、年齢のめやすに応じた体操提示。
- ゲームHUD整理。
- 体操画面。
- たいそうのきろく・ミッション。
- ブース紹介・出展者レポート。
- 会場マップの全面UI再設計。

データはRepository越しのfixture/local adapterでよい。Figmaは作らない。

完了条件: 375×667と390×844で主要フローが通り、旧UIより明確に改善したことを画面比較できる。

### フェーズ2 — Cloudflare・LINE認証（7/17〜7/22・仮決定）

- Cloudflare preview環境。
- Product Worker。
- LIFF init/login/friendship。
- LINE token検証とsession。
- 流入元。

完了条件: 未友だちではゲーム不可、追加済みでは内部利用者を一意に確定。

### フェーズ3 — Supabase縦貫通（7/20〜7/26・仮決定）

- 承認済みmigration。
- アンケート、ゲーム、体操、進捗、mission、event API。
- 同意API、ブース・マップ・ローテーションAPI。
- オフライン端末キューと再送（機能-208・P0）。
- RLS・管理者認証。
- 出展者レポート集計。

完了条件: Write→Read→Displayと権限分離が正常・異常系で通る。

### フェーズ4 — Harness（7/24〜7/28・仮決定）

- version固定。
- LINE Harnessテスト環境。
- LINE health、Webhook、最小シナリオ。
- source付きLINE導線。
- リッチメニュー「事業者の方へ」と事業者タグ（決定-013）。
- （P1）セグメントタグ同期の名簿upsert土台（機能-407）。

完了条件: 実LINEテストアカウントで友だち追加→LIFFまで通る。

### フェーズ5 — 統合デモ（7/29〜8/1・仮決定。8/2がYourTIME本番）

- デモ台本。
- テストデータ初期化。
- iOS/Android実機。
- 障害時fallback。
- 画面録画と操作証跡。

### フェーズ6 — IG Harness追加デモ（8/2以降または並行余力時・仮決定）

- 管理API keyの保管方式とCORSを補正。
- データ削除処理と固定fallback値を確認・補正。
- 自社InstagramプロアカウントとMeta Appを接続。
- コメントまたはDM→LINE→LIFFを通す。

完了条件: 安全性Gateと実アカウントのスモークテストを通過する。初回コアデモの完了条件にはしない。

## 4. コンポーネント境界

```text
src/
  app/              ルーティング・認証・アプリシェル
  game/             ゲームロジック・Pixi描画・HUD
  exercise/         体操定義・プレイヤー・完了
  onboarding/       歓迎・アンケート・キャラ登場
  progress/         記録・mission・streak
  village/          キャラ・ブース・会場
  reports/          利用者/出展者/運営表示
  integrations/     LIFF・Product API・Harness境界
  shared/           型、UI、Repository、計測
```

既存ディレクトリは一括移動せず、画面改修時に責務単位で段階移行する。

## 5. UI実装ルール

- 1画面1主目的、主CTAは原則1つ。
- 絵文字を最終UI素材にしない。
- すべてを白い磨りガラスカードにしない。
- ゲーム中は盤面を第一階層にする。
- 長い画面はルート単位でスクロール可能にする。
- `body overflow:hidden`を全画面へ強制しない。
- オレンジ上の白文字など、基準未達の配色を使わない。
- タップ、フォーカス、disabled、loading、error、offlineを定義する。
- `aria-pressed`、`aria-expanded`、dialog focus、progress semanticsを実装する。
- reduced motionと音声オフを後付けにしない。

## 6. データ実装ルール

- ブラウザから個人データtableへservice roleで直アクセスしない。
- LINEの生識別子を保存・表示・ログ出力しない。
- event UUIDで冪等にする。
- streakとmissionはサーバーを正とし、端末値を信用しない。
- unknownなevent name、property、survey answerを拒否する。
- D1とSupabaseの全同期を作らない。
- デモデータに`data_mode=demo`を持たせる。

## 7. 外部連携実装ルール

- Webhookは署名→strict入力→永続dedupe→業務処理の順。
- request IDを入口から外部API・DBまで伝播する。
- child timeoutはparent SLAより3秒以上短くする。
- 一斉配信、DM、投稿は本番で人の承認を挟む。
- Harnessは検証済みtag/commitへ固定する。
- secretを`VITE_`環境変数へ置かない。

## 8. 仕様台帳

| 仕様 | 状態 | 備考 |
|---|---|---|
| 本ディレクトリ | current | 統合SSoT |
| `docs/30_suwapuyo/01_spec.md` | reference | ゲーム詳細を参照 |
| `docs/30_suwapuyo/03_retention-loop-spec.md` | superseded部分あり | LINE任意、隠しキャラ・旧解放条件は本仕様で廃止 |
| `docs/30_suwapuyo/07_line-required-liff-spec.md` | merged | LINE必須判断を本仕様へ統合 |
| `docs/40_yourtime-platform/03_village-concierge-design.md` | reference | アンケート・マップの既存設計 |
| `docs/40_yourtime-platform/06_design-guideline.md` | superseded部分あり | 世界観は継承、UI原則は本仕様優先 |
| `docs/40_yourtime-platform/07_experience-and-user-journey.md` | reference | 最上位体験原則を継承 |
| `docs/10_fuwafuwa-land/09_exhibitor-report-and-platform-growth-spec.md` | reference | 出展者価値と集計を継承 |

## 9. 次の実装開始条件

- 本仕様のMVP範囲に合意。
- 家族アンケートの質問項目に合意（2026-07-11 合意済み。決定-016）。
- キャラクター登場単位に合意（2026-07-11 合意済み。決定-019）。
- Cloudflare、LINE、Meta、Supabaseの対象環境を区別。
- DB migrationと外部デプロイは実行前承認。

これらが揃うまでは、ローカルのUIデモとRepository境界までは進めてよい。外部設定や本番データへ推測で進まない。

## 10. GPT/Codex開発モデルの使い分け

本節は開発作業の品質・速度・コストを調整するための運用であり、利用者向けアプリからGPT APIを呼び出す仕様ではない。具体的な永続指示は`AGENTS.md`を正とする。

| クラス | 主な担当 | 必須となる場面 |
|---|---|---|
| A: 最上位 | 設計、セキュリティ、プライバシー、DB、外部連携、SSoT矛盾解消、最終レビュー | 子どもデータ、認証・認可、本番設定、破壊的変更、公開前判断 |
| B: 標準 | 通常実装、UI、テスト、リファクタ、文書同期 | 原則の実装担当。A領域へ入ったら昇格 |
| C: 軽量 | 横断検索、一覧化、定型変換、一次整理 | 独立した機械的作業のみ。B以上の再確認必須 |

運用原則:

- モデル名を設計へ埋め込まず、利用可能な最上位・標準・軽量モデルへ読み替える。
- 現行候補はA=`gpt-5.6-sol`、B=`gpt-5.6-terra`、C=`gpt-5.6-luna`。選択画面で利用できない場合は同等クラスを使う。
- Cだけで仕様確定、完了判定、セキュリティ判断、本番反映を行わない。
- Aの判断も、要確認Gate、専門家監修、木幡さんの事業判断を代替しない。
- 並列化は独立した調査・ファイル・検証に限定し、共有状態や同一ファイルの競合を避ける。
- 作業完了時はセルフレビューと検証を行い、commit、push、Cloudflare deploy、LINE反映を個別に報告する。
