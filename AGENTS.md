# すわぷよ開発エージェント運用ルール

このファイルは、Codex等の開発エージェントが本リポジトリで作業するときの永続指示である。アプリ本体からGPT APIを呼び出す仕様ではない。

## 着手前の必須確認

1. `~/projects/taskboard/data/tasks.json`から対象タスクの最新版を読む。
2. `docs/70_すわぷよ・ユアタイム統合仕様/07_サマリー/03_要求・期限別要件・実装状態.md`と、対象タスクの「正本」に記載されたSSoTを読む。未確定事項を推測で補完しない。
3. `git status`と直近コミットを確認し、利用者や別エージェントの未コミット変更を保全する。
4. 完了済みタスクを再実装しない。`docs/_履歴/`内の旧タスク一覧は経緯確認にだけ使う。
5. 利用者が見えるバグ・違和感を修正する場合は、可能なら修正前に`docs/80_制作過程/`の規約で証拠と判断過程を残す。

## 実行環境（恒久ルール）

- このリポジトリは **WSL2 の Ubuntu-24.04** 上にある。Linux系コマンド、Git、npm、テスト、build、lint、スクリプト実行はすべてWSL内で実行する。
- Windows側のUNCパスを作業ディレクトリにして直接`npm`や`git`を実行しない。必要に応じて、`wsl.exe -d Ubuntu-24.04 -- bash -lc "cd /home/ykoha/projects/suwapuyo && <command>"` を使う。
- コマンドの実行結果は、WSL内で実行したものだけを検証結果として報告する。

## 開発モデルの使い分け

モデル名はアプリの依存仕様にせず、作業の難易度と事故影響で選ぶ。現在の選択肢に該当モデルがない場合は、同じ役割の「最上位・標準・軽量」モデルへ読み替える。

| クラス | 現行の第一候補 | 使用する作業 | 使用しない完了判定 |
|---|---|---|---|
| A: 最上位 | `gpt-5.6-sol`または利用可能な最強のcoding/reasoningモデル | アーキテクチャ、認証・認可、個人情報、DB migration、外部API、本番障害、SSoT矛盾解消、最終設計レビュー | なし。高リスク作業の最終責任を持つ |
| B: 標準 | `gpt-5.6-terra`または利用可能なbalanced codingモデル | 通常実装、UI、テスト、リファクタ、文書更新、調査、レビュー修正 | A対象を単独で確定しない |
| C: 軽量 | `gpt-5.6-luna`または利用可能なfast/cost-efficientモデル | `rg`横断検索、一覧化、定型変換、旧文言候補抽出、テストログの一次整理 | 仕様決定、セキュリティ判断、完了判定、本番反映を単独で行わない |

### 昇格ルール

- B/Cで開始しても、認証、個人情報、子どもデータ、課金、DB破壊、外部公開、LINE/Cloudflare本番設定へ到達したらAへ昇格する。
- Cの成果は必ずB以上が原文・差分・テスト結果を確認する。
- Aを使っても専門家確認や木幡さんの意思決定Gateを代替しない。
- 複数エージェントは独立ファイルまたは独立検証だけを並列化する。同じファイル、同じ状態管理、同じデプロイを同時編集しない。

## 作業単位の進め方

1. 対応TODO、対象SSoT、変更対象、非対象を宣言する。
2. 実装前に未確定Gateがないか確認する。あれば選択肢と推奨案を提示して止める。
3. 実装後にセルフレビューし、型、単体テスト、build、`git diff --check`を変更リスクに応じて実行する。
4. 完了報告では、変更、検証、未実施、外部反映の有無を分離する。
5. commit、push、Cloudflare Pages deploy、LINE反映を混同せず、それぞれ実行結果を明記する。
6. 会話全文やエージェント内部ログをGitへ複製せず、公開可能な事実・判断・証拠へ構造化する。

## 現行固有の禁止事項

- 本番はCloudflare Pagesの`suwapuyo`。Vercelを本番扱いしない。
- CHECKIN-106/107の本番schema・署名・RLSはGate通過前に実装しない。
- CHAR-302は木幡さんが再抽選タイミングを選ぶまで配線しない。
- ONBOARD-303は要確認-011の年齢境界が確定するまで実装しない。
- LINE-204〜209、BOOTH-102/103/106/107、MAKER-101/106の外部素材・実機・運用値を推測しない。
